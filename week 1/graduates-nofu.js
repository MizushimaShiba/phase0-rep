// declare variable and also assign value
let nama = 'hey'
let nilai = 82
let absensi = 6

// IF nilai is bigger than or equal to 70 AND absensi is smaller to or equal to 5
if (nilai >= 70 && absensi <= 5) {
  // Display lulus
  console.log(`Saudara ${nama}, anda lulus`)
  // ELSE
} else {
  // Display tidak lulus
  console.log(`Saudara ${nama}, anda tidak lulus`)
}